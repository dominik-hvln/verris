# Używane przez Prisma CLI (migrate / generate) z katalogu libs/database.
# Skopiuj do libs/database/.env — DATABASE_URL musi być taki sam jak w apps/api/.env

DATABASE_URL=postgresql://verris:verris_password@localhost:5433/verris_db?schema=public
