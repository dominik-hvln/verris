# Base URL of the Verris NestJS API the panel talks to (server-side fetch).
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional product modules (Sprint B — default off for start LIVE scope)
# NEXT_PUBLIC_FEATURE_ECO=true
# NEXT_PUBLIC_FEATURE_IAM=true
# NEXT_PUBLIC_FEATURE_REFERRAL=true
