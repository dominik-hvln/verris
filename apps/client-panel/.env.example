# Base URL of the Verris NestJS API the panel talks to (server-side fetch).
NEXT_PUBLIC_API_URL=http://localhost:3000

# Product modules (Sprint B): EKO + referral ON by default; IAM opt-in
# NEXT_PUBLIC_FEATURE_ECO=false
# NEXT_PUBLIC_FEATURE_REFERRAL=false
# NEXT_PUBLIC_FEATURE_IAM=true
