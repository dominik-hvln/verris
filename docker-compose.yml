services:
  postgres:
    image: postgres:18.6-alpine
    container_name: verris_postgres
    restart: always
    environment:
      POSTGRES_USER: verris
      POSTGRES_PASSWORD: verris_password
      POSTGRES_DB: verris_db
    ports:
      - '5433:5432'
    volumes:
      # DEV-01: nowy wolumen. Stary `postgres_data` zainicjowano innym POSTGRES_USER, a obraz
      # Postgresa czyta POSTGRES_USER/PASSWORD tylko przy pustym katalogu danych — kontener wstawał
      # „zdrowy”, a logowanie odbijało (rola verris nie istniała). Stary wolumen zostaje nietknięty.
      # PB-38: Postgres 18 trzyma dane w /var/lib/postgresql/18/docker i nie wystartuje z wolumenem
      # pod starą ścieżką /var/lib/postgresql/data — stąd nowy wolumen (lokalna baza startuje pusta:
      # `pnpm db:migrate:deploy && pnpm db:seed`). Stare wolumeny zostają nietknięte.
      - postgres_data_18:/var/lib/postgresql
    healthcheck:
      # Logowanie tymi samymi danymi co aplikacja — pg_isready nie sprawdza roli ani hasła.
      test: ['CMD-SHELL', 'PGPASSWORD=verris_password psql -h 127.0.0.1 -U verris -d verris_db -c "select 1" >/dev/null']
      interval: 5s
      timeout: 3s
      retries: 20

  # PB-38: Valkey (fork Redisa, BSD) — ten sam protokół i port.
  redis:
    image: valkey/valkey:9.1.2-alpine
    container_name: verris_redis
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - valkey_data:/data

  # ---------------------------------------------------------------------------
  # MinIO — S3-compatible object storage for ticket attachments, data
  # exports (RODO), DPA PDFs and (later) generated invoices.
  #
  # Console UI: http://127.0.0.1:9001  (default user/pass below — change in prod)
  # S3 API:     http://127.0.0.1:9000
  # ---------------------------------------------------------------------------
  minio:
    # PB-38: obraz budowany ze źródeł (obrazy minio/minio zniknęły z Docker Huba) — pierwsze `up` kompiluje ~2–3 min.
    build: ops/docker/minio
    image: verris-minio:RELEASE.2025-10-15T17-29-55Z
    container_name: verris_minio
    restart: always
    command: ['server', '/data', '--console-address', ':9001']
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-verris-dev}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-verris-dev-password}
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio_data:/data
    healthcheck:
      test: ['CMD', 'curl', '-fsS', 'http://127.0.0.1:9000/minio/health/live']
      interval: 10s
      timeout: 3s
      retries: 5

  # One-shot bootstrapper that creates the buckets we need + a 7-day
  # lifecycle policy for data-exports. Idempotent: if a bucket already
  # exists, `mc mb -p` is a no-op. Container exits 0 once buckets are ready.
  minio-bootstrap:
    image: verris-minio:RELEASE.2025-10-15T17-29-55Z
    container_name: verris_minio_bootstrap
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-verris-dev}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-verris-dev-password}
    entrypoint: >
      /bin/sh -c "
        set -e;
        mc alias set verris http://minio:9000 \"$$MINIO_ROOT_USER\" \"$$MINIO_ROOT_PASSWORD\";
        mc mb -p verris/verris-ticket-attachments;
        mc mb -p verris/verris-data-exports;
        mc mb -p verris/verris-dpa-pdfs;
        mc mb -p verris/verris-invoices;
        mc anonymous set none verris/verris-ticket-attachments;
        mc anonymous set none verris/verris-data-exports;
        mc anonymous set none verris/verris-dpa-pdfs;
        mc anonymous set none verris/verris-invoices;
        mc ilm rule add --expire-days 7 verris/verris-data-exports || true;
        echo 'MinIO buckets ready.';
      "
    restart: 'no'

volumes:
  postgres_data:
  postgres_data_v2:
  postgres_data_18:
  redis_data:
  valkey_data:
  minio_data:
